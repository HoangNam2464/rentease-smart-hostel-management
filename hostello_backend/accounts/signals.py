"""
Account signals are intentionally empty during the RentEase migration.

Owner profiles are created explicitly in Django Admin so the legacy hostel
warden/student role flow does not run for new RentEase users.
"""
